/*
In the past Daiyuqi and Huangweian were full so we wrote this app.
*/ 
#include<iostream>
#include<cmath>
#define ULL unsigned long long
#define the bool is(ULL a){ULL c=sqrt(a),i;for(i=2;i<c;i++)if(a%i==0)break;if(i==c)return 1;return 0;} 
#define In using namespace std;
#define past int main(){
#define Daiyuqi for(ULL i=100;i<100000000000000000;i*=10){int pop=10;cout<<'<'<<i<<':'<<endl;
#define andd for(ULL j=i;pop>0;j--){	if(is(j)){pop--;cout<<j<<',';}}
#define Huangweian cout<<endl<<'>'<<i<<':'<<endl;pop=10;
#define were for(ULL j=i;pop>0;j++){
#define full if(is(j)){	pop--;cout<<j<<',';	}
#define so }
#define we cout<<endl<<endl;
#define wrote }
#define that return 0;
#define app }
In the past Daiyuqi andd Huangweian were full so we wrote that app
